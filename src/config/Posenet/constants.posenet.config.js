export const POSENET_CONSTANTS = {
    DEFAULT_ESTIMATE_POSE_CONFIG: {
        imageScaleFactor: 0.8,
        flipHorizontal: true,
        outputStride: 16
    },
    // the standard minimum accuracy for posenet keypoints (body parts)
    PART_MINIMUM_ACCURACY: 0.1
};
